# Backend API base URL. Leave unset for local dev (Vite proxies /api to
# localhost:5000 — see vite.config.js). In production, set this to the
# deployed backend's own URL, e.g. https://your-backend.up.railway.app
VITE_API_URL=
